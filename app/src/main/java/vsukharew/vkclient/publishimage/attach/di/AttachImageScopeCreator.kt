package vsukharew.vkclient.publishimage.attach.di

import vsukharew.vkclient.common.di.ScopeCreator

object AttachImageScopeCreator : ScopeCreator() {
    override val parentScopes: List<ScopeData> = emptyList()
}